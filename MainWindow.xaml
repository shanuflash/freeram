<Window x:Class="FreeRam2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="FreeRam 2.0" Height="712" Width="430" 
        Background="#050507" FontFamily="Segoe UI Variable Display" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="45" GlassFrameThickness="0" CornerRadius="15" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#5A5A70"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1A1A24"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="0,15,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#1C1C26" BorderThickness="1" CornerRadius="15" Background="#0A0A0F" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="#0D0D14" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" Margin="20,0" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="#00E676" MouseLeftButtonDown="EngineToggle_Click" Cursor="Hand" ToolTip="Click to toggle engine on/off"/>
                    <TextBlock Text="FREERAM // 2.0" Foreground="#5A5A70" FontSize="11" FontWeight="Bold" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Style="{StaticResource WindowControlButton}" Click="Minimize_Click" FontSize="16"/>
                    <Button Content="✕" Style="{StaticResource CloseButtonStyle}" Click="Close_Click" FontSize="12"/>
                </StackPanel>
            </Grid>

            <StackPanel Grid.Row="1" Margin="30,15,30,25">
                <Grid Margin="0,10,0,30">
                    <Ellipse Width="190" Height="190" Stroke="#151522" StrokeThickness="10"/>
                    <Ellipse Width="190" Height="190" Stroke="#00E676" StrokeThickness="10" StrokeDashArray="12 2" StrokeDashCap="Round">
                        <Ellipse.Effect>
                            <DropShadowEffect Color="#00E676" BlurRadius="20" ShadowDepth="0" Opacity="0.4"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock x:Name="RamUsageText" Text="0%" Foreground="White" FontSize="62" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,0,0,-5"/>
                        <TextBlock Text="RAM LOAD" Foreground="#00E676" FontSize="11" FontWeight="Black" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Button x:Name="CleanNowButton" Click="CleanNowButton_Click" Height="52" Cursor="Hand" Margin="0,0,0,30" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="#00B0FF" CornerRadius="10">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="⚡" Foreground="White" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="INITIATE PURGE" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#0091EA"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Border Background="#0F0F18" CornerRadius="15" Padding="20" BorderBrush="#1C1C26" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="CORE TRIGGERS" Foreground="#4A4A60" FontSize="10" FontWeight="Black" Margin="0,0,0,20"/>
                        <Grid Margin="0,0,0,18">
                            <TextBlock Text="THRESHOLD" Foreground="#8A8A9A" FontSize="11" FontWeight="Bold" VerticalAlignment="Top"/>
                            <TextBlock x:Name="ThresholdText" Text="85%" Foreground="#00E676" FontSize="11" HorizontalAlignment="Right" FontWeight="Bold"/>
                            <Slider x:Name="ThresholdSlider" Margin="0,22,0,0" Minimum="50" Maximum="99" Value="85" IsSnapToTickEnabled="True" TickFrequency="1" ValueChanged="ThresholdSlider_ValueChanged"/>
                        </Grid>
                        <Grid Margin="0,0,0,18">
                            <TextBlock Text="RECOVERY" Foreground="#8A8A9A" FontSize="11" FontWeight="Bold" VerticalAlignment="Top"/>
                            <TextBlock x:Name="RecoveryText" Text="70%" Foreground="#00B0FF" FontSize="11" HorizontalAlignment="Right" FontWeight="Bold"/>
                            <Slider x:Name="RecoverySlider" Margin="0,22,0,0" Minimum="30" Maximum="95" Value="70" IsSnapToTickEnabled="True" TickFrequency="1" ValueChanged="RecoverySlider_ValueChanged"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="COOLDOWN" Foreground="#8A8A9A" FontSize="11" FontWeight="Bold" VerticalAlignment="Top"/>
                            <TextBlock x:Name="CooldownText" Text="5m" Foreground="#FF9100" FontSize="11" HorizontalAlignment="Right" FontWeight="Bold"/>
                            <Slider x:Name="CooldownSlider" Margin="0,22,0,0" Minimum="1" Maximum="30" Value="5" IsSnapToTickEnabled="True" TickFrequency="1" ValueChanged="CooldownSlider_ValueChanged"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#0D0D14" CornerRadius="10" Padding="15" Margin="0,25,0,0" BorderBrush="#1C1C26" BorderThickness="1">
                    <Grid>
                        <TextBlock x:Name="StatusText" Text="SYSTEM ONLINE" Foreground="#4A4A60" FontSize="11" FontWeight="Black" VerticalAlignment="Center"/>
                        <Button x:Name="EngineToggleButton" Click="EngineToggleButton_Click" HorizontalAlignment="Right" Cursor="Hand" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="toggleBorder" Background="#00E676" CornerRadius="6" Padding="12,4">
                                        <TextBlock x:Name="toggleText" Text="ON" Foreground="#0A0A0F" FontSize="10" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="toggleBorder" Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>